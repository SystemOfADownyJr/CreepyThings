import React from 'react'

let isAuthor = true
const postList = ({post, user}) => {
    return post.map(post => {
        Author()
    })
}
function Author() {
    if (isAuthor) {
        <button type="button" onClick={() => user(post)}>
              Delete
            </button>
            //append to a div?
            //recover post._id?
    }
}

export default postList